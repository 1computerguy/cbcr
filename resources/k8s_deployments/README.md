# Kubernetes Range Deployment Configurations

This folder is to hold any static configurations, and is where the dynamically generated configs will be stored. To deploy all the configs in this directory, once they are generated using the kubectl command.

`kubectl create -f $K8S_CONFIGS`