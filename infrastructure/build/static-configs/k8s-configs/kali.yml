---
apiVersion: apps/v1beta1
metadata:
  namespace: default
  name: kali-test
kind: Deployment
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: kali-test
      annotations:
        k8s.v1.cni.cncf.io/networks: na-svc
    spec:
      volumes:
      - nfs:
          path: /configs/kali
          server: storage
        name: nfs-kali-data
      containers:
      - name: kali-test
        image: master:5000/kali
        securityContext:
          privileged: true
        volumeMounts:
        - mountPath: /data
          name: nfs-kali-data
        env:
        - name: IP_ADDR
          value: "8.1.2.3"
        - name: LEN
          value: "8"
        - name: GATEWAY
          value: "8.0.0.1"
